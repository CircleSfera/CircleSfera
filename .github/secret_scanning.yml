paths-ignore:
  # Redaction test fixtures: intentionally Stripe/JWT/DB-credential-shaped
  # strings used to verify the app's own log-scrubbing regex actually
  # catches them. Not real secrets — see the test names in this file.
  - "circlesfera-backend/src/common/testing/logging-redaction.spec.ts"
