/**
 * Home For You (`GET /feed/foryou`) experiment keys.
 * Assignment is on User.id via ExperimentsService (FeatureFlag % + UserExperiment).
 * Missing flag row → control (current hybrid ranking).
 */
export const FEED_HOME_FOLLOWING_FIRST = 'feed_home_following_first';
