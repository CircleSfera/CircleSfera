# Creation Flow & Photo Editor Overhaul

## Goal

Substantially improve the user experience for creating Posts, Stories, and Frames, with a specific focus on upgrading the `PhotoEditor` to include manual image adjustments.

## User Review Required

None. These are enhancements to existing features.

## Proposed Changes

### [Frontend] [Components]

#### [MODIFY] [PhotoEditor.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/components/PhotoEditor.tsx)

- **Add Tabs**: Switch between "Filters" and "Adjust" modes.
- **Add Slider Controls**: Implement manual sliders for:
  - Brightness
  - Contrast
  - Saturation
  - Sepia
  - Grayscale
- **Real-time Preview**: Apply CSS filters dynamically based on slider values combined with the selected preset.

#### [MODIFY] [CreatePostModal.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/components/CreatePostModal.tsx)

- **Story Mode Improvements**:
  - Enforce/Suggest 9:16 aspect ratio.
  - Improve the "Add Music" and "Close Friends" UI.
- **UI Polish**:
  - Enhance transitions between steps.
  - Improve the drop zone visualization.

## Verification Plan

### Manual Verification

1.  Open `/create` or click "New Post".
2.  Upload an image.
3.  Click "Edit" (or entering edit mode automatically).
4.  Verify "Filters" tab works as before.
5.  Switch to "Adjust" tab.
6.  Verify sliders change the image appearance in real-time.
7.  Save and verify the final image has the edits applied (visually, noting that currently we might just be saving the filter string, so we need to ensure the backend or frontend stores these adjustment values too).
    - _Note_: The current `Media` type might only support a single `filter` string. I might need to serialize the adjustments into a JSON string or CSS string and store it in the `filter` field, or update the backend to support structured edit data. **For now, I will append the CSS values to the filter string.**
