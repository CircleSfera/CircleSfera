# Authentication Flow Improvements

The goal is to resolve issues with login and registration by providing better error feedback and allowing more flexible login options.

## User Review Required

> [!IMPORTANT]
> I will be updating the login logic to accept both **email** and **username**. This requires a small change in the backend `AuthService`.

## Proposed Changes

### [Component] Backend API

- #### [MODIFY] [auth.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/backend-api/src/auth/auth.service.ts)
  - Update `login` method to search by either `email` (User) or `username` (Profile).
  - Ensure clear error messages are thrown (already done, but verified).

### [Component] Frontend Application

- #### [MODIFY] [Login.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/frontend-app/src/pages/Login.tsx)
  - Change "Email" label/placeholder to "Email or username".
  - Change input type from `email` to `text` to allow username login.
  - Display actual error messages from the backend (e.g., `error.response?.data?.message`).

- #### [MODIFY] [Register.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/frontend-app/src/pages/Register.tsx)
  - Display actual error messages from the backend (e.g., "Username already taken").
  - Add simple frontend validation feedback if possible.

## Verification Plan

### Manual Verification

1. Try to log in with an existing username (`user1`).
2. Try to register with an existing email and verify the error message says "Email already registered".
3. Try to register with a username shorter than 3 characters and verify the error message.
