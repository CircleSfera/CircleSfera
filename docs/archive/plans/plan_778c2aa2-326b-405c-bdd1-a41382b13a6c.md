# Integrate SendGrid for Transactional Emails

We will replace the generic SMTP transport with the official SendGrid SDK for better deliverability and ease of use, as the user already has a configured account.

## Proposed Changes

### Dependencies

#### [MODIFY] [package.json](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/package.json)

- Add `@sendgrid/mail` to dependencies.

### Environment Configuration

#### [MODIFY] [.env](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/.env)

- Add `SENDGRID_API_KEY`.
- (Cleanup) We can keep `SMTP_FROM` and `SMTP_FROM_NAME` as they are used by the service for the "from" field.

### Backend Implementation

#### [MODIFY] [email.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/email/email.service.ts)

- Refactor to use `@sendgrid/mail` SDK.
- Update `sendMail` private method to call SendGrid API.

## Verification Plan

### Manual Verification

1. Install dependencies and restart containers.
2. Trigger an email (registration or reset).
3. Verify in backend logs that the SendGrid API successfully accepted the message.

# Admin Dashboard Enhancements

We will expand the current Admin Dashboard to go beyond report management, providing a unified interface for platform health monitoring and moderation.

## Proposed Changes

### Backend Infrastructure

#### [NEW] [admin.module.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/admin/admin.module.ts)

- Define the module and register `AdminService` and `AdminController`.

#### [NEW] [admin.controller.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/admin/admin.controller.ts)

- `GET /api/v1/admin/stats`: Returns counts for users, posts, stories, and active reports.
- `GET /api/v1/admin/users`: Paginated list of users with ban/unban controls.
- `GET /api/v1/admin/posts`: Paginated list of posts with moderation controls.

#### [NEW] [admin.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/admin/admin.service.ts)

- Logic for fetching statistics and managing entities from an administrative perspective.

### Frontend UI

#### [MODIFY] [Admin.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/pages/Admin.tsx)

- Refactor to use a tabbed interface (Analytics, Reports, Users, Posts).
- Implement new components for User Management and Post Gallery.

## Verification Plan

### Automated Tests

- Create unit tests for `AdminService` ensuring correct statistic calculation.
- Add admin endpoints to E2E test suite.

### Manual Verification

1. Login with an admin account.
2. Verify that all tabs load correct data.
3. Test banning a user from the "Users" tab.
4. Test deleting a post from the "Posts" tab.

# User Management

We will provide administrative access to specific users as requested by the platform owner.

## Proposed Changes

### Database Updates

- Grant `ADMIN` role to specific accounts (e.g., `user1`).

## Verification Plan

### Manual Verification

1. Run the `grant-admin.ts` script.
2. Log in with the target account and verify access to the Admin Dashboard.
