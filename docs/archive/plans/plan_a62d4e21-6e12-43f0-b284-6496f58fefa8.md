# Implementation Plan - Fix Socket Authentication

Robustify socket handshake authentication by improving cookie parsing and ensuring consistent cookie name usage.

## Proposed Changes

### Backend: Socket Gateway

#### [MODIFY] [app.gateway.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/socket/app.gateway.ts)

- Replace manual cookie parsing with the `cookie` library for robust handling of whitespaces and edge cases.
- Use `ACCESS_TOKEN_COOKIE`, `REFRESH_TOKEN_COOKIE`, and `CSRF_COOKIE_NAME` constants consistently.
- Improved debug logging to help identify future issues.

```typescript
// Proposed parsing logic
const cookieHeader = client.handshake.headers.cookie;
if (cookieHeader) {
  const cookies = cookie.parse(cookieHeader);
  // ... check keys accurately ...
}
```

## Verification Plan

### Automated Tests

- Since this is a gateway handshake issue, I will verify it by running the backend and checking the logs.
- I'll check if a mock socket connection with various cookie formats (with/without spaces) can be authenticated.

### Manual Verification

1. Start the backend and frontend.
2. Log in to the application.
3. Open the browser console and check if the socket connects successfully.
4. Verify that the `AppGateway` logs show `access=true, refresh=true, csrf=true`.
5. Test behavior when the access token expires (wait or manually clear it) to ensure the `socketStore.ts` refresh logic works with the improved parser.
