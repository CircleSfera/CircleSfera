# Enhancing Admin Users UI

The goal is to improve the user administration section in the frontend by replacing simple native browser `confirm()` implementations with custom premium modals, and adding slight visual improvements to the users table.

## Proposed Changes

1. **Frontend Repository (`circlesfera-frontend`)**:
   - #### [MODIFY] [Admin.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/pages/Admin.tsx)
     - Manage modal state (`confirmAction` state to track action type and item ID).
     - Import and integrate `ConfirmModal` (`src/components/modals/ConfirmModal.tsx`).
     - Replace `confirm('...') && mutation.mutate(...)` on the ActionButtons of the Users and Posts tables with `setConfirmAction({ type, id })`.
     - Implement the `<ConfirmModal />` component near the bottom of the rendered UI with dynamic titles, messages, and colors depending on if it's a ban, unban, or delete post action.

## Verification Plan

- Run the development server and navigate to `/admin`.
- Try to ban/unban a user. A beautiful custom modal should appear instead of the native JS dialog.
- The same should happen when deleting a post.
