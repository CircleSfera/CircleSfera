# Admin Access Security Hardening

Implement a robust frontend navigation guard and fix the backend profile response to ensure only users with the `ADMIN` role can access the `/admin` dashboard.

## Proposed Changes

### [Backend] CircleSfera Backend

#### [MODIFY] [profiles.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/profiles/profiles.service.ts)
- Update `getProfile` and `getMyProfile` to include `role: true` in the Prisma `select` statement for the `user` relation.

---

### [Shared] CircleSfera Shared

#### [MODIFY] [index.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-shared/src/models/index.ts)
- Update `ProfileWithUser` interface to include `role?: string` in the nested `user` object.

---

### [Frontend] CircleSfera Frontend

#### [NEW] [AdminGuard.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/components/auth/AdminGuard.tsx)
- Create a new guard component that:
  - Accesses the current user profile from `useAuthStore`.
  - Redirects to `/` if the user is not authenticated or does not have the `ADMIN` role.
  - Renders children only if the user is an admin.

#### [MODIFY] [App.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/App.tsx)
- Wrap the `/admin` route with the new `AdminGuard`.

## Verification Plan

### Automated Tests
- N/A (Manual verification on production dev environment).

### Manual Verification
1. Login with a standard user account on `dev.circlesfera.com`.
2. Attempt to navigate manually to `/admin`.
3. Verify the app redirects you back to the home page or shows an error.
4. Login with an admin account and verify the dashboard is accessible.
