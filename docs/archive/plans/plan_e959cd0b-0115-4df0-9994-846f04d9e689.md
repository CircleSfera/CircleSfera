# Implementation Plan - Project Completion

The goal is to complete the missing functionality identified during the comprehensive review. The frontend is largely complete, but the backend lags in supporting Audio attachments for posts/stories and real-time Chat reactions.

## User Review Required

> [!IMPORTANT]
> I will be modifying the backend `AppGateway` to handle `send_reaction` events which are currently emitted by the frontend but ignored by the backend.

## Proposed Changes

### Backend - Audio Integration

#### [MODIFY] [create-post.dto.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/posts/dto/create-post.dto.ts)

- Add optional `audioId` field to `CreatePostDto`.

#### [MODIFY] [create-story.dto.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/stories/dto/create-story.dto.ts)

- Add optional `audioId` field to `CreateStoryDto`.

#### [MODIFY] [posts.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/posts/posts.service.ts)

- Update `create` method to include `audioId` when creating the post.

#### [MODIFY] [stories.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/stories/stories.service.ts)

- Update `create` method to include `audioId` when creating the story.

### Backend - Chat Reactions

#### [MODIFY] [chat.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/chat/chat.service.ts)

- Add `addReaction(messageId: string, userId: string, reaction: string)` method.
- This method should:
  - Check if reaction already exists.
  - If so, verify if it's the same (ignore) or different (update/replace?).
  - Actually, typically we just toggle or add. The simplified version: create or update `MessageReaction`.
  - Return the reaction object.

#### [MODIFY] [app.gateway.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/socket/app.gateway.ts)

- Import `ChatService`.
- Add `@SubscribeMessage('send_reaction')` handler.
- Extract `messageId`, `reaction`, `recipientId` from payload.
- Call `chatService.addReaction`.
- Emit `message_reaction` event to the recipient (and sender/room) to update UI.

#### [MODIFY] [app.module.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/app.module.ts)

- Ensure `ChatModule` exports `ChatService` or `AppGateway` has access to it. (Likely `AppGateway` is in `SocketModule` which imports `ChatModule` or vice versa. I need to check module structure to avoid circular dependencies).
  - _Correction_: `AppGateway` is likely in `SocketModule`. `ChatService` is in `ChatModule`. `ChatModule` probably imports `SocketModule`. If `AppGateway` needs `ChatService`, we might have a circular dependency.
  - _Alternative_: Move reaction logic to `ChatController` and have Frontend call an API endpoint `POST /chat/messages/:id/reactions` instead of socket?
  - _But Frontend_ (`ChatWindow.tsx`) strictly uses `socket.emit('send_reaction')`.
  - _Best Path_: Inject `ChatService` into `AppGateway`. If circular dependency arises, I will use `ModuleRef` or forward reference.

## Verification Plan

### Automated Tests

- I will run the backend build to ensure no type errors.
- `npm run build` in `circlesfera-backend`.

### Manual Mocks

- I can create a script `test-reaction.ts` that connects to the socket and emits `send_reaction`, then listens for `message_reaction` to verify the loop.
- I can create a script `test-create-post-audio.ts` that calls the API to create a post with a mock `audioId`.

### Browser Verification

- Open the frontend.
- **Chat**: Open a chat, send a message. Click the emoji reaction button (hover over message). Verify the reaction appears.
- **Post**: Create a new post/story, select music (mock or real if available), and publish. Verify via DB or API that `audioId` is saved.
