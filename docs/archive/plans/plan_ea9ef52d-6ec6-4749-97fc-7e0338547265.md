# [ARCHIVED] Fix: Verification Badge and Admin Responsiveness
> [!NOTE]
> This plan was successfully executed and verified in production on 2026-04-01.

This plan addresses the missing verification badges in the UI and the layout issues in the Admin panel for mobile devices.

## Proposed Changes

### [Frontend]

#### [MODIFY] [AdminTable.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/components/admin/AdminTable.tsx)
- Refactor the `Table` component to avoid mixing `tr` and `div` elements.
- Introduce a mechanism to render desktop rows and mobile cards separately, or use a more robust responsive table pattern.

#### [MODIFY] [AdminSidebar.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/components/admin/AdminSidebar.tsx)
- Adjust the sidebar width to be responsive (`w-full lg:w-64`).
- Move the sidebar to a collapsible state or a horizontal scroll on mobile to save vertical space.

#### [MODIFY] [Admin.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/pages/Admin.tsx)
- Simplify the header for mobile.
- Ensure the layout grid switches properly between stack and side-by-side.

#### [MODIFY] [PostHeader.tsx](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-frontend/src/components/post/PostHeader.tsx) and others
- Ensure `verificationLevel` is being passed correctly.
- Check if other components (like `SuggestedUserCard` or `Profile`) are missing the `VerificationBadge`.

### [Backend]

#### [MODIFY] [posts.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/posts/posts.service.ts)
- Double check if `verificationLevel` needs to be explicitly selected in some queries if they use `select` instead of `include`. (Currently seems fine, but will verify).

## Verification Plan

### Manual Verification
- Resize the browser or use DevTools to verify the Admin panel on mobile (375px) and tablet (768px).
- Log in as the admin user and verify the shield/badge appears on posts and profile.
- Verify the mobile "cards" in the Admin tables show all relevant information.
