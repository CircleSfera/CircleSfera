# Fix Backend Lint and Formatting Errors

This plan addresses several linting and formatting errors reported in the backend codebase, specifically in `jwt-optional.guard.ts` and `posts.service.ts`.

## Proposed Changes

### Auth Component

#### [MODIFY] [jwt-optional.guard.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/auth/guards/jwt-optional.guard.ts)

- Remove the unused `// @ts-expect-error` directive on line 6.

---

### Posts Component

#### [MODIFY] [posts.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/posts/posts.service.ts)

- Reformat ternary expressions for `likes` inclusion in `findAll`, `getFramesFeed`, `findOne`, and `findByUser` methods to span multiple lines as requested by the lint rules.
- Add parentheses to the `post` argument in `.map()` calls.
- Correct indentation from 4 spaces to 2 spaces in the affected blocks.
- Add missing trailing commas in objects.

## Verification Plan

### Automated Tests

- I will run the linting command for the backend to ensure all reported errors are resolved.
- Command: `npm run lint` (in `circlesfera-backend`)

### Manual Verification

- None required as these are purely formatting and minor type-related fixes.
