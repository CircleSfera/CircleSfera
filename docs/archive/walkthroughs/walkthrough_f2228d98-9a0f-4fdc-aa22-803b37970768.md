# Walkthrough - Backend Lint Fixes

I have resolved the linting and formatting errors reported in the backend.

## Changes Made

### Auth Component

#### [jwt-optional.guard.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/auth/guards/jwt-optional.guard.ts)

- Removed an unused `// @ts-expect-error` directive that was causing a lint error.

### Posts Component

#### [posts.service.ts](file:///Users/ShadyFeliu/Desktop/Projects/CircleSfera/circlesfera-backend/src/posts/posts.service.ts)

- Realigned code formatting with project ESLint/Prettier rules.
- Fixed indentation in several methods (`findAll`, `getFramesFeed`, `findByUser`).
- Formatted ternary expressions to span multiple lines as required.
- Added parentheses to arrow function arguments in `.map()` calls.
- Ensured consistent use of trailing commas.

## Verification Results

### Automated Tests

- Ran `npm run lint` in the `circlesfera-backend` directory.
- **Result**: Command exited with code 0 (Success), indicating no remaining lint or formatting issues.

```bash
> circlesfera-backend@1.0.0 lint
> eslint "{src,apps,libs,test}/**/*.ts" --fix
# No errors reported
```
